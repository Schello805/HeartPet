<%- include('../partials/top', { pageTitle }) %>

<section class="card border-0 shadow-sm">
  <div class="card-body">
    <div class="prose-block"><% String(content || '').split(/\r?\n/).forEach((line, index, lines) => { %><%= line %><% if (index < lines.length - 1) { %><br /><% } %><% }) %></div>
  </div>
</section>

<%- include('../partials/bottom') %>
